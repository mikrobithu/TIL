
== velero
:toc:
:source-highlighter: rouge


[source,shell]
----
$ kubectl -n velero get backups.velero.io daily-backup-20260119190349 -o json | jq '.status|"errors: \(.errors) warnings: \(.warnings)"'
"errors: 63 warnings: 113"

$ kubectl -n velero create -f - << EOF
apiVersion: velero.io/v1
kind: DownloadRequest
metadata:
  name: log-20260119190349
spec:
  target:
    kind: BackupLog
    name: daily-backup-20260119190349
EOF
----
# 1
kubectl -n velero get downloadrequest log-20260119190349 -o jsonpath='{.status.phase}'
Processed # <-- its ready
# 2
kubectl -n velero get downloadrequest log-20260119190349 -o jsonpath='{.status.downloadURL}'
# 3
curl -kL '<downloadURL>' -o daily-backup-20260119190349-logs.gz
